package backend.ajude.Enum;

/**
 * Criado para representar os Status das campanhas, podendo ser ATIVA, ENCERRADA, VENCIDA ou CONCLUIDA 
 */
public enum StatusCampanha{
    ATIVA, ENCERRADA, VENCIDA, CONCLUIDA
}